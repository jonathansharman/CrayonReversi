// Interface for a Reversi player.
class Player {
	// Makes a move for the game's current player.
	function makeMove(game) {
		throw new Exception("Abstract method not overridden!");
	}

	function toString() {
		throw new Exception("Abstract method not overridden!");
	}
}
